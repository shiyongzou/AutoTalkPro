name: Build Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-macos:
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.6'
      - name: Fix DNS for CocoaPods downloads
        run: |
          echo "nameserver 8.8.8.8" | sudo tee /etc/resolver/sqlite.org
          echo "nameserver 1.1.1.1" | sudo tee -a /etc/resolver/sqlite.org
          # Pre-resolve and add to hosts as fallback
          IP=$(dig +short www.sqlite.org @8.8.8.8 | head -1)
          if [ -n "$IP" ]; then
            echo "$IP www.sqlite.org" | sudo tee -a /etc/hosts
          fi
      - run: flutter pub get
      - run: flutter build macos --release
      - name: Package
        run: |
          cd build/macos/Build/Products/Release
          zip -r "$GITHUB_WORKSPACE/AutoTalkPro-macOS.zip" "Auto Talk Pro.app"
      - uses: actions/upload-artifact@v4
        with:
          name: AutoTalkPro-macOS
          path: AutoTalkPro-macOS.zip

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.6'
      - run: flutter pub get
      - run: flutter build windows --release
      - name: Package
        shell: pwsh
        run: |
          Compress-Archive -Path "build\windows\x64\runner\Release\*" -DestinationPath "AutoTalkPro-Windows.zip"
      - uses: actions/upload-artifact@v4
        with:
          name: AutoTalkPro-Windows
          path: AutoTalkPro-Windows.zip

  release:
    needs: [build-macos, build-windows]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      # GitHub Release
      - uses: softprops/action-gh-release@v2
        with:
          body: |
            ## AutoTalk Pro ${{ github.ref_name }}

            - macOS (Intel / Apple Silicon)
            - Windows x64

            📖 [新手教程](https://gitee.com/zoushiyong_admin/AutoTalkPro/blob/main/docs/tutorial.md)
          files: |
            artifacts/AutoTalkPro-macOS/AutoTalkPro-macOS.zip
            artifacts/AutoTalkPro-Windows/AutoTalkPro-Windows.zip

      # Gitee Release（国内下载）
      - name: Create Gitee Release
        env:
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
          TAG_NAME: ${{ github.ref_name }}
        if: env.GITEE_TOKEN != ''
        run: |
          RELEASE_ID=$(curl -s -X POST "https://gitee.com/api/v5/repos/zoushiyong_admin/AutoTalkPro/releases" \
            -H "Content-Type: application/json" \
            -d "{
              \"access_token\": \"${GITEE_TOKEN}\",
              \"tag_name\": \"${TAG_NAME}\",
              \"name\": \"${TAG_NAME}\",
              \"body\": \"AutoTalk Pro ${TAG_NAME}\\n\\n- macOS (Intel/Apple Silicon)\\n- Windows x64\\n\\n📖 [新手教程](https://gitee.com/zoushiyong_admin/AutoTalkPro/blob/main/docs/tutorial.md)\",
              \"prerelease\": false
            }" | python3 -c "import sys,json; print(json.load(sys.stdin).get('id',''))")

          echo "Gitee Release ID: $RELEASE_ID"

          if [ -n "$RELEASE_ID" ] && [ "$RELEASE_ID" != "None" ]; then
            curl -s -X POST "https://gitee.com/api/v5/repos/zoushiyong_admin/AutoTalkPro/releases/${RELEASE_ID}/attach_files" \
              -H "Content-Type: multipart/form-data" \
              -F "access_token=${GITEE_TOKEN}" \
              -F "file=@artifacts/AutoTalkPro-macOS/AutoTalkPro-macOS.zip"

            curl -s -X POST "https://gitee.com/api/v5/repos/zoushiyong_admin/AutoTalkPro/releases/${RELEASE_ID}/attach_files" \
              -H "Content-Type: multipart/form-data" \
              -F "access_token=${GITEE_TOKEN}" \
              -F "file=@artifacts/AutoTalkPro-Windows/AutoTalkPro-Windows.zip"

            echo "Gitee Release uploaded successfully"
          else
            echo "Gitee Release creation failed, skipping upload"
          fi
